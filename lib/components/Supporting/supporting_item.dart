import 'package:flutter/material.dart';

class Supporting extends StatelessWidget {
  const Supporting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
